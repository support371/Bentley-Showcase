# Bentley Showcase backend environment
# Do not commit real secrets.

BACKEND_VERSION=1.0.0
CORS_ORIGINS=https://bentley-showcase.vercel.app,http://localhost:3000,http://localhost:5173

# Set with wrangler secret put in production
PASSWORD_SALT=replace-with-random-secret
SEED_SECRET=replace-with-random-secret
SEED_OWNER_EMAIL=owner@example.com
SEED_OWNER_PASSWORD=replace-with-temporary-strong-password

# Frontend / Vite
VITE_API_BASE_URL=https://bentley-webpage.<your-workers-subdomain>.workers.dev
